---
import Item from './Item.astro';
const items = Astro.props.items || [];
console.log('Lista items:', items);
---

<ul class="text-gray-500 space-y-3 mb-5">
	{ items.map( (item) => (
		<Item>{item}</Item> 
	))}
	<slot />
</ul>
